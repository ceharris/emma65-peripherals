[project]
name = "emma65-via"
version = "0.1.0"
description = "Client for the emma65 VIA peer protocol, shared by VIA-based peripherals"
authors = [
    { name = "Carl Harris", email = "ceharris414@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
